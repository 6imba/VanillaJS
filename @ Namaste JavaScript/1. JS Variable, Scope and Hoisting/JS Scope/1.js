// - In vanilla javascript of web browser the global object is the browser window.
// - A global object is an object that always exists in the global scope. 
// - So how many global object does vanilla js have? IS it just window object or there are other many too?

// # Environment -> GlobalScope -> GlobalObject -> window -> var.

// #Environment: GlobalObject
// Browser: window
// Node: global

